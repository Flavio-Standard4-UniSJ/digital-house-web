<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
  
    <% if(lista.length <= 0){ %>
      <p>Nada a ser mostrado.</p>
    <% }else{ %>
      <% lista.forEach(item=>{ %>   
        <ul>
          <li><%= item.username %></li>
        </ul>
      <% }) %>
    <% } %>
    
  </body>
</html>
