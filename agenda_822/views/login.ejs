<%- include('./global/head.ejs') %>
</head>
<body>
    <header>
        <nav>
            <img src="/images/logo.png" alt="agenda 822">
            <a href="/login" target="_self"><img src="/images/entrar.svg"></a>
            <h1><%= title %></h1>
        </nav>
        <hr>
    </header>
    <main>
        <section>
            <article>
                <% if(typeof email != "undefined" && typeof senha != "undefined"){ %>
                    <div><p><%= email %> AND <%= senha %> <a href="/">Voltar para Home</a>  </p></div>
                <% } else{ %>   
                <form action="/restrito" method="POST">
                    <label for="email">email</label>
                    <input type="text" name="email" id="email">
                    <label for="senha">senha</label>
                    <input type="password" name="senha" id="senha">
                    <input type="submit" name="entrar" value="entrar">
                </form>
                <% } %>
                <div>
                    <a href="#" target="_self" title="recuperar senha">esqueci minha senha</a>
                </div>
            </article>
        </section>
<%- include('./global/footer.ejs') %>
      